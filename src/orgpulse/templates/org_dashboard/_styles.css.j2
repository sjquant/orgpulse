  <style>
    .org-dashboard__author-button {
      border: 1px solid var(--line-strong);
      background: var(--panel-subtle);
      color: var(--text);
      border-radius: var(--radius-control);
      padding: 8px 12px;
      font: inherit;
      cursor: pointer;
    }

    .org-dashboard__author-button:hover,
    .org-dashboard__author-button--active {
      background: var(--active-fill);
      border-color: var(--accent);
    }

    .org-dashboard__author-button:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .org-dashboard__detail-shell {
      display: grid;
      gap: 14px;
    }

    .org-dashboard__people-grid {
      display: grid;
      grid-template-columns: minmax(280px, 0.36fr) minmax(0, 0.64fr);
      grid-template-areas:
        "people-controls people-controls"
        "people-ranking profile-detail";
      gap: 16px;
      align-items: start;
    }

    .org-dashboard__people-grid > * {
      min-width: 0;
    }

    .org-dashboard__people-ranking-controls {
      grid-area: people-controls;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 16px;
      align-items: start;
    }

    .org-dashboard__people-ranking-controls .report__tab-strip--primary {
      width: 100%;
    }

    .org-dashboard__people-ranking-controls .report__tab-strip--secondary {
      justify-self: end;
    }

    .org-dashboard__people-ranking-shell {
      grid-area: people-ranking;
      display: grid;
      gap: 12px;
    }

    .org-dashboard__person-list {
      display: grid;
      gap: 10px;
    }

    .org-dashboard__person-card {
      width: 100%;
      min-width: 0;
      display: grid;
      gap: 8px;
      border: 1px solid var(--line);
      border-radius: var(--radius-control);
      padding: 12px;
      background: var(--panel-subtle);
      color: var(--text);
      font: inherit;
      text-align: left;
      cursor: pointer;
    }

    .org-dashboard__person-card--reviewer-rank {
      cursor: default;
    }

    .org-dashboard__person-card:hover,
    .org-dashboard__person-card--active {
      border-color: var(--line-strong);
      border-left-color: var(--accent);
      background: var(--theme-toggle-hover);
    }

    .org-dashboard__person-card--reviewer-rank:hover {
      border-color: var(--line);
      border-left-color: transparent;
      background: var(--panel-subtle);
    }

    .org-dashboard__person-card-header,
    .org-dashboard__person-card-metrics,
    .org-dashboard__profile-summary {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      align-items: baseline;
      min-width: 0;
    }

    .org-dashboard__person-card-metrics,
    .org-dashboard__profile-summary {
      flex-wrap: wrap;
    }

    .org-dashboard__person-card strong {
      font-size: 1rem;
    }

    .org-dashboard__person-card small,
    .org-dashboard__profile-summary span {
      color: var(--muted);
      font-weight: 700;
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }

    .org-dashboard__person-card-metrics span {
      color: var(--muted);
      font-size: 0.82rem;
    }

    .org-dashboard__person-profile {
      grid-area: profile-detail;
      display: grid;
      gap: 14px;
      min-width: 0;
    }

    .org-dashboard__person-profile > *,
    .org-dashboard__detail-shell,
    .org-dashboard__detail-grid,
    .org-dashboard__profile-subgrid {
      min-width: 0;
      max-width: 100%;
    }

    .org-dashboard__person-profile .org-dashboard__detail-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .org-dashboard__person-profile .report__chart-root {
      aspect-ratio: 16 / 5;
      min-height: 220px;
    }

    .org-dashboard__person-profile .report__chart-root svg {
      min-height: 0;
    }

    .org-dashboard__profile-subgrid {
      display: grid;
      grid-template-columns: minmax(0, 1.35fr) minmax(0, 0.9fr) minmax(0, 0.75fr);
      gap: 14px;
    }

    .org-dashboard__detail-tabs {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .org-dashboard__detail-grid {
      display: grid;
      grid-template-columns: repeat(6, minmax(0, 1fr));
      gap: 10px;
    }

    .org-dashboard__detail-card {
      border: 1px solid var(--line);
      border-radius: var(--radius-control);
      padding: 12px;
      background: var(--panel-subtle);
    }

    .org-dashboard__detail-card strong {
      display: block;
      margin-bottom: 6px;
      color: var(--muted);
      font-size: 0.72rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
    }

    .org-dashboard__detail-card span {
      font-size: 1.1rem;
      font-weight: 700;
    }

    .org-dashboard__state-list {
      display: grid;
      gap: 10px;
    }

    .org-dashboard__state-row {
      display: grid;
      grid-template-columns: 160px minmax(0, 1fr) 70px;
      gap: 12px;
      align-items: center;
    }

    .org-dashboard__state-track {
      height: 10px;
      background: var(--track);
      border-radius: var(--radius-pill);
      overflow: hidden;
    }

    .org-dashboard__state-fill {
      height: 100%;
      background: linear-gradient(90deg, var(--accent-3), var(--accent));
    }

    .report__small-note {
      color: var(--muted);
      font-size: 0.82rem;
    }

    .org-dashboard__control-note {
      color: var(--muted);
      line-height: 1.5;
      font-size: 0.86rem;
    }

    .report__support-grid,
    .org-dashboard__reference-grid,
    .org-dashboard__reference-stack {
      display: grid;
      gap: 12px;
    }

    .report__support-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .org-dashboard__control-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 12px;
    }

    .org-dashboard__control-actions {
      display: flex;
      align-items: center;
      gap: 10px;
      flex-wrap: wrap;
    }

    .org-dashboard__search-input {
      min-width: 220px;
      max-width: 100%;
      border: 1px solid var(--line-strong);
      background: var(--panel-subtle);
      color: var(--text);
      border-radius: var(--radius-control);
      padding: 9px 12px;
      font: inherit;
    }

    .org-dashboard__search-input::placeholder {
      color: var(--muted);
    }

    .org-dashboard__search-input:focus-visible {
      outline: 2px solid var(--accent);
      outline-offset: 2px;
    }

    .org-dashboard__reference-grid {
      grid-template-columns: 1fr;
    }

    .org-dashboard__reference-stack {
      grid-template-columns: 1fr;
    }

    .org-dashboard__disclosure {
      border: 1px solid var(--line);
      border-radius: var(--radius-control);
      background: var(--panel-subtle);
      overflow: hidden;
    }

    .org-dashboard__disclosure > summary {
      list-style: none;
      cursor: pointer;
      padding: 14px 16px;
      font-weight: 700;
      font-family: var(--font-display);
      border-bottom: 1px solid transparent;
    }

    .org-dashboard__disclosure[open] > summary {
      border-bottom-color: var(--line);
    }

    .org-dashboard__disclosure > summary::-webkit-details-marker {
      display: none;
    }

    .org-dashboard__disclosure > :not(summary) {
      padding: 14px 16px 16px;
    }

    .org-dashboard__disclosure > .report__tab-strip--secondary {
      margin-left: auto;
    }

    @media (max-width: 1320px) {
      .org-dashboard__detail-grid {
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }
    }

    @media (max-width: 1180px) {
      .org-dashboard__profile-subgrid,
      .report__support-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 1080px) {
      .org-dashboard__person-profile .report__chart-root {
        aspect-ratio: 16 / 6;
      }
    }

    @media (max-width: 860px) {
      .org-dashboard__people-grid {
        grid-template-columns: 1fr;
        grid-template-areas:
          "people-controls"
          "people-ranking"
          "profile-detail";
      }

      .org-dashboard__people-ranking-controls {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 720px) {
      .org-dashboard__detail-grid,
      .org-dashboard__person-profile .org-dashboard__detail-grid {
        grid-template-columns: 1fr;
      }

      .org-dashboard__control-actions {
        width: 100%;
        justify-content: stretch;
      }

      .org-dashboard__search-input {
        width: 100%;
        min-width: 0;
      }

      .org-dashboard__state-row {
        grid-template-columns: 1fr;
      }
    }
  </style>
