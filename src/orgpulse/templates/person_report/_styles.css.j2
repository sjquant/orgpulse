  <style>
    .person-report .report__hero-panel {
      margin-bottom: 20px;
    }

    .person-report .report__summary-strip {
      grid-template-columns: repeat(auto-fit, minmax(min(250px, 100%), 1fr));
    }

    .person-report .report__section-panel {
      margin-bottom: 16px;
    }

    .person-report .person-report__repository-focus-grid {
      position: relative;
      z-index: 0;
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 14px;
      padding: 8px;
    }

    .person-report #repositories .report__table-wrap {
      position: relative;
      z-index: 1;
    }

    .person-report #repositories .report__table-wrap:has(.report__metric-tooltip:hover),
    .person-report #repositories .report__table-wrap:has(.report__metric-tooltip:focus),
    .person-report #repositories .report__table-wrap:focus-within {
      z-index: 30;
      overflow: visible;
    }

    .person-report .person-report__repository-focus-card {
      display: grid;
      gap: 10px;
      border: 1px solid var(--line);
      border-radius: var(--radius-control);
      padding: 16px 18px;
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.05), transparent 62%),
        var(--panel-raised);
      box-shadow: var(--shadow-tight);
    }

    .person-report .person-report__repository-focus-card strong {
      font-family: var(--font-display);
      overflow-wrap: anywhere;
    }

    .person-report .person-report__repository-focus-card span {
      color: var(--muted);
      font-size: 0.86rem;
      line-height: 1.45;
    }

    .person-report .person-report__table-heading {
      display: flex;
      justify-content: space-between;
      gap: 14px;
      align-items: start;
      margin-bottom: 14px;
    }

    .person-report .person-report__table-heading > div {
      min-width: 0;
    }

    .person-report .person-report__table-heading .report__period-state-pill {
      flex-shrink: 0;
    }

    .person-report td:first-child {
      font-weight: 700;
      color: var(--text);
    }

    .person-report .person-report__metric-split {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 18px;
    }

    .person-report .person-report__chart-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 16px;
    }

    .person-report .person-report__chart-panel {
      display: grid;
      gap: 18px;
    }

    .person-report .person-report__chart-card {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 14px;
      min-width: 0;
      border: 1px solid var(--line);
      border-radius: var(--radius-control);
      padding: 18px;
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.052), transparent 62%),
        var(--panel-raised);
      box-shadow: var(--shadow-tight);
    }

    .person-report .person-report__chart-card h3 {
      margin: 0;
      font-family: var(--font-display);
      font-size: 1.05rem;
    }

    .person-report .person-report__chart-toolbar {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      align-items: start;
    }

    .person-report .report__chart-root {
      aspect-ratio: 16 / 6;
    }

    .person-report .person-report__org-comparison-dot {
      background: color-mix(in srgb, var(--accent-2) 72%, var(--muted) 28%);
      opacity: 0.72;
    }

    .person-report .person-report__comparison-switch {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 4px 9px 4px 5px;
      min-height: 28px;
      border: 1px solid var(--line-strong);
      border-radius: var(--radius-pill);
      background:
        linear-gradient(180deg, rgba(255, 255, 255, 0.045), transparent),
        var(--panel-raised);
      color: var(--text);
      font: inherit;
      font-size: 0.82rem;
      line-height: 1;
      cursor: pointer;
    }

    .person-report .person-report__comparison-switch[aria-checked="false"] {
      color: var(--muted);
      border-color: var(--line);
      background: transparent;
    }

    .person-report .person-report__comparison-switch:focus-visible {
      outline: 2px solid color-mix(in srgb, var(--accent-2) 72%, transparent);
      outline-offset: 2px;
    }

    .person-report .person-report__comparison-switch-track {
      position: relative;
      width: 34px;
      height: 18px;
      flex: 0 0 auto;
      border: 1px solid var(--line);
      border-radius: var(--radius-pill);
      background: color-mix(in srgb, var(--muted) 26%, transparent);
      transition: background-color 0.16s ease, border-color 0.16s ease;
    }

    .person-report .person-report__comparison-switch-thumb {
      position: absolute;
      top: 2px;
      left: 2px;
      width: 12px;
      height: 12px;
      border-radius: 50%;
      background: var(--muted);
      box-shadow: 0 1px 2px rgba(0, 0, 0, 0.24);
      transition: transform 0.16s ease, background-color 0.16s ease;
    }

    .person-report .person-report__comparison-switch[aria-checked="true"] .person-report__comparison-switch-track {
      border-color: color-mix(in srgb, var(--accent-2) 64%, var(--line) 36%);
      background: color-mix(in srgb, var(--accent-2) 28%, transparent);
    }

    .person-report .person-report__comparison-switch[aria-checked="true"] .person-report__comparison-switch-thumb {
      background: color-mix(in srgb, var(--accent-2) 82%, white 18%);
      transform: translateX(16px);
    }

    .person-report .person-report__comparison-switch-label {
      white-space: nowrap;
    }

    .person-report .person-report__org-comparison-line,
    .person-report .person-report__org-comparison-point {
      color: color-mix(in srgb, var(--accent-2) 72%, var(--muted) 28%);
    }

    .person-report .person-report__org-comparison-line {
      opacity: 0.64;
    }

    .person-report .person-report__org-comparison-point {
      opacity: 0.7;
    }

    .person-report #person-trend-grain-tabs,
    .person-report #person-cadence-tabs {
      justify-content: flex-end;
      align-self: start;
      margin-left: auto;
    }

    .person-report .person-report__cadence-grid {
      display: grid;
      grid-template-columns: minmax(0, 1fr);
      gap: 14px;
    }

    .person-report .person-report__cadence-panel {
      display: grid;
      gap: 12px;
      min-width: 0;
    }

    .person-report .person-report__cadence-panel h3 {
      margin: 0;
      font-family: var(--font-display);
      font-size: 1rem;
    }

    @media (max-width: 1180px) {
      .person-report .person-report__repository-focus-grid,
      .person-report .person-report__metric-split,
      .person-report .person-report__chart-toolbar,
      .person-report .person-report__cadence-grid {
        grid-template-columns: 1fr;
      }

      .person-report .person-report__chart-toolbar {
        display: grid;
      }

      .person-report #person-trend-grain-tabs,
      .person-report #person-cadence-tabs {
        justify-content: flex-end;
      }
    }

    @media (max-width: 720px) {
      .person-report .person-report__table-heading {
        display: grid;
      }
    }
  </style>
